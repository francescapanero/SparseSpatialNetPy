# eventually I shall make a class for the graph.


class graphmodel:

    def __init__(self, type, alpha, sigma, tau):
        self.type == type
        if type == "GGP":
            self.param.alpha = alpha
            self.param.sigma = sigma
            self.param.tau = tau

        if type == "exptiltBFRY":


        if type == "doublepl":

